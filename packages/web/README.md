# @squads/web

Reserved for the Squads frontend. Empty for now — the webapp build has not
started. The frontend will read pool state from the `Squads` contract
(`packages/contracts`) and cross-reference the [Megapot Data
API](https://api.megapot.io/v1) for win stats and leaderboards.
